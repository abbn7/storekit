[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build:production"]

[start]
cmd = "APP_ROOT=/app FRONTEND_DIST=artifacts/storekit/dist/public NODE_ENV=production sh scripts/start-production.sh"
